Takes an airport dataset with several metrics of data, extends it to reach 100,000 data points. The information is sorted based on the inputs given by the user, either prompted through a console/SFML menu.
To run the project
1. Clone repo
2. Build cmake file (all necessary sfml stuff is done locally to be portable)
3. run main
